# In-memory SQLite database for the tests: the real reservation.db is never touched.
# The single pooled connection keeps the database alive while the Spring context runs.
spring.datasource.url=jdbc:sqlite:file:reservation-test?mode=memory&cache=shared&foreign_keys=on
